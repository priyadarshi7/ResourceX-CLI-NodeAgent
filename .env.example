# Used by docker compose (copy to .env in repo root)
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGIN=*
# For inter-device training from Studio uploads (your PC LAN IP):
# PUBLIC_URL=http://192.168.1.10:4000
